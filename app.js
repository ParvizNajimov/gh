const mainForm = document.forms.secions
const mainFormInput = mainForm.nameInput

mainForm.addeventListener('submit', function (event) {
    if (required(mainFormInput)) {
       mainFormInput.perentelement.insert
    }
})